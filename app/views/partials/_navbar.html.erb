<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><i class="glyphicon glyphicon-th-list"></i> Reports</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li><a href="#">Link</a></li>
        <% end %>  
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if !user_signed_in? %>
           <li><a href="/users/sign_up" class="btn btn-xs"><i class="glyphicon glyphicon-pencil"></i> Sign Up</a></li>
           <li><a href="/users/sign_in" class="btn btn-xs"><i class="glyphicon glyphicon-log-in"></i> Log In</a></li>
        <% else %>     
           <% if current_user.admin? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin? <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><a href="/admin"><i class="glyphicon glyphicon-cog"></i> Admin Dashboard</a> </li>
              </ul>
            </li> 
              
           <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Options <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="/users/edit" class="btn btn-xs"><i class="glyphicon glyphicon-edit"></i> Edit Login Info</a></li>
              <li role="separator" class="divider"></li>
             <li><%= link_to '<i class="glyphicon glyphicon-log-out"></i> Log Out'.html_safe, destroy_user_session_path, method: :delete, class: "btn btn-xs" %></li>
            </ul>
          </li>
           <li class="navbar-text"><%= current_user.name %> <%= current_user.surname %></li>
            <% if @user.avatar? %>
              <%= image_tag @user.avatar.url(:thumb), class: "avatar-mini" %>
            <% else %>
              <img src="../avatar.png" alt="No Avatar" class="avatar-mini">
            <% end %>   
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>