<% if !user_signed_in? %>
	<h1>Welcome</h1>
<% else %>
	<h1 class="text-center">Hello, <%= current_user.name %> <%= current_user.surname %> </h1>
<% end %>	
<hr>

<% if !user_signed_in? %>
	<h4 class="text-center">Please <%= link_to "Log In", new_user_session_path %> or <%= link_to "Sign Up", new_user_registration_path %> to continue.</h4>
<% else %>
	<h4>Finished with your reports? <%= link_to "Log Out", destroy_user_session_path, method: :delete %></h4>
	<%= current_user.employee_user_level_id %>	
<% end %>

